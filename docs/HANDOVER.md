# Master Handover, Decisions & Milestone Index

## 1. Repository Status & Orientation

- **Active Branch**: `main`
- **Repository Mission**: Deterministic telemetry engine, near real-time watcher daemon, and visual analytics dashboard for Antigravity token consumption, context caching efficiency, rolling quota allowances, and inference costs across Antigravity workspaces.
- **Upstream Data Dependency**: `~/.gemini/antigravity/conversations/*.db` (Read-only, zero external APIs).
- **Working Agreement**: [`AGENTS.md`](../AGENTS.md) (Canonical).
- **Active Acceptance Gates**: [`VDONE.md`](../VDONE.md).

---

## 2. Master Decision Index (Architectural Decision Records)

| ADR ID | Decision Title | Status | Summary |
|---|---|---|---|
| **ADR-001** | Strict Read-Only SQLite Access (`?mode=ro`) | **Adopted** | All connections to Antigravity databases must use URI read-only syntax to prevent locking or corrupting live IDE sessions. |
| **ADR-002** | Pure-Python Wire Protobuf Decoding | **Adopted** | Decode varints and length-delimited wire types directly in pure standard Python to avoid external dependencies or protoc compilation. |
| **ADR-003** | Dual-Mode Dashboard Execution | **Adopted** | Support both a single-file, zero-dependency offline HTML dashboard (`open dashboard/index.html`) and a near real-time live watcher daemon (`watch_telemetry.py`). |
| **ADR-004** | Byte-Deterministic HTML Export Data Hook | **Adopted** | Generated dashboard outputs inject sorted, deterministic JSON into `<script id="injected-dashboard-data">` to maintain 100% clean git trees without timestamp drift. |
| **ADR-005** | Configurable Model Pricing Tier Matrix | **Adopted** | Pricing rates per million tokens are maintained in `config/pricing.json` with fallback defaults for new or unmapped model IDs. |
| **ADR-006** | Verifiable Acceptance Gates & Milestone Slices | **Adopted** | Non-trivial work requires explicit executable gates in `VDONE.md` (`CHECK` and `EXPECT`) and durable slice records in `docs/milestones/`. |
| **ADR-007** | 3-Tier Subagent Allocation & Pre-Flight Disclosure | **Adopted** | Strict model tiering (`flash_lite` mechanical, `flash` engineering, `inherit`/`pro` architecture), zero-collision file scopes, and visible chat pre-flight disclosures. |
| **ADR-008** | Proactive Context Boundary Calling & Handoff | **Adopted** | Proactively call milestone boundaries upon completion, commit with a clean tree, and instruct fresh session handoff to avoid context degradation. |
| **ADR-009** | Rolling Quota & Subscription Value Dual-Track Modeling | **Adopted** | Differentiate between included Pro subscription quota (5h rolling burst, 1w rolling aggregate, $0 marginal cost) and out-of-pocket AI credit burn. |
| **ADR-010** | Hybrid Overage Detection & Model-Specific Credit Reconciliation | **Adopted** | Reconcile Google One AI Credit overage billing ($0.01 / credit) using model-specific compute rates (2.5 credits/turn on Flash) and fusing log-anchored exhaustion (`language_server.log`) with continuous sliding window age-out math. |
| **ADR-011** | Multi-Currency Cost Accounting & British Pounds Calibration (£23.99 / 2,500 Credits) | **Adopted** | Default to GBP (£) accounting calibrated against the user's purchased Google One AI credit plan (£23.99 for 2,500 credits = £0.009596 / credit) alongside standard USD ($0.0100 / credit). Support instant client-side toggling across all cards, allowance matrices, and hourly reconciliation tables. |
| **ADR-012** | High-Contrast Light & Dark Visual Themes with Single-File Deployment | **Adopted** | Provide interactive client-side CSS-variable theme switching (`[ ☀️ Light | 🌙 Dark ]`) with `localStorage` persistence and zero-flicker pre-render script in a unified single-file `dashboard/index.html` deployment. |
| **ADR-013** | Debounced Dual-Source Telemetry Watcher & User Daemon Architecture | **Adopted** | Monitor Antigravity SQLite WAL logs and runtime logs concurrently, debouncing exporter triggers with a 2-second settle window and starvation guard, resilient to SQLite locks, packaged as a macOS LaunchAgent background service. |
| **ADR-014** | Signal-Anchored 5-Hour Overage Engine & Calendar Cycle Anchoring | **Adopted** | Anchor AI credit deduction exclusively to confirmed 429 server exhaustion intervals in language_server.log, eliminating phantom deductions; align weekly quota window to Thursday 19:00 BST / 18:00 UTC calendar resets and establish 24th monthly Google One renewal horizon. |
| **ADR-015** | Decoupled Monthly Pro Subscription & Prepaid Overage Credit Bank Architecture | **Adopted** | Fully decouple the monolithic subscription concept into two distinct financial layers: Tier 1 (Fixed Google One AI Premium subscription £18.99/mo / $19.99/mo covering standard quota usage with imputed API savings/ROI metrics) and Tier 2 (Prepaid £23.99 / 2,500 AI credits add-on bank at £0.0096/credit debited strictly when quota bursts or outside quota limits). |
| **ADR-016** | Persistent Append-Only 429 Exhaustion Ledger Defense | **Adopted** | Ingest persistent append-only records from `data/exhaustion_ledger.json` in `src/log_reader.py` to reconcile official Google One billing deductions (1,179 credits burned across two distinct burst intervals) resilient against upstream `language_server.log` file truncations on IDE restarts. |
| **ADR-017** | Reasoning Effort Model ID Multiplexing & Empirical Benchmark Taxonomy | **Adopted** | Antigravity routes reasoning intensity sliders to distinct model IDs (`1318` Flash High vs. `1319` Flash Medium); Medium operates with adaptive thinking (down to 0 tokens on standard code synthesis); calibrated pricing taxonomy across Gemini Flash (High/Medium), Gemini 3.1 Pro High (`1016`), Claude Sonnet/Opus Thinking on Vertex (`1035`/`1026`), and GPT OSS Medium (`342`). |
| **ADR-018** | Dedicated Local Telemetry Vault, Hybrid Lossless Archival & Lineage Topology Architecture | **Adopted** | Permanent local SQLite database (`data/antigravity_vault.db`) storing full decoded text and raw protobuf blobs (hybrid, zero `.pkl`), workspace attribution from `agyhub_summaries_proto.pb`, parent-child subagent lineage (`invoke_subagent`), tool and skill execution indexing (`tool_calls_archive`, `skills_archive`), `language_server.log` truncation protection, and universal screenshot ingestion skill (`credit-statement-ingest`). |
| **ADR-019** | Dual-Track Provider Quota Silos (Gemini vs. Claude/GPT) & Step-Level Routing | **Adopted** | Quotas in Antigravity operate across two distinct, independent provider tracks: Track 1 (Gemini Models: Flash, Pro, Subagent Lite/Fast) anchored to Thursday 19:00 BST cycle reset and 5h burst with Google One credit spillover, and Track 2 (Claude Sonnet/Opus & GPT models) anchored to an independent 7-day rolling window age-out and 5h burst with hard-blocking (zero credit spillover). Telemetry routing strictly operates at the step level (`step_type = 15`) by `pricing.json` family to prevent cross-track contamination in heterogeneous parent-subagent swarms. Unified dual-card Antigravity UI representation. |
| **ADR-020** | Autonomous Subagent 3-Tier Routing Matrix & 19-Model Telemetry Census | **Adopted** | Subagents decouple from the desktop selector, routing "flash_lite" to 1050 (Flash Lite), "flash" to 1322 (Fast Agent Assistant), and "pro" to 1036 (Pro Low Reasoning, deliberately avoiding the 11k-token thinking floor of Pro High). Full 115-database census confirms all 19 historical model IDs calibrated in config/pricing.json with zero unmapped models. |
| **ADR-021** | Official Google Rate Card Quota Weighting & Live Language Server RPC Synchronization | **Adopted** | Quota consumption scales proportionally to model token cost (Flash $0.75/$3.75 1.0x baseline, Pro $2.00/$12.00 ~2.8x, Flash-Lite $0.25/$1.50 ~0.35x, with 90% prompt cache discount); calibrated 5h ($20.00) and weekly ($133.00) cost capacities converge against upstream Connect-RPC RetrieveUserQuotaSummary endpoint. |
| **ADR-022** | Multi-Project & Git Branch Telemetry Intelligence Architecture | **Adopted** | Extract authoritative workspace root paths and active Git branches directly from `trajectory_metadata_blob` (`Field 1:sf1`, `sf4`, `Field 7`) inside conversation SQLite databases. Deliver hierarchical project- and branch-level token costing, cache efficiency metrics, and session drill-downs in a dedicated "Projects & Git Branches" dashboard tab. |
| **ADR-023** | Subscription Coverage vs. Actual Overage & Cost Avoidance Accounting | **Adopted** | Differentiate between turns covered under standard Google One / Antigravity Pro subscription (£18.99/mo) at £0.00 marginal spend (98.2% across audited workspaces) and actual out-of-pocket overage credit deductions (1,179 credits / £11.31) attributed exclusively to confirmed 429 server exhaustion intervals. Reframe theoretical developer API costs as Avoided Cost (Plan Value ROI) to highlight subscription savings. |
| **ADR-024** | Decoupled Telemetry Data Hook (`data.js`) & Clean Presentation Isolation | **Adopted** | Decouple mutable telemetry JSON projections from the tracked `dashboard/index.html` into a dedicated, git-ignored `dashboard/data.js` (`window.__TELEMETRY_DATA__ = {...};`). Preserves 100% offline browser execution under `file:///` without CORS restrictions, eliminates 260,800+ lines of context and git diff bloat, keeps the tracked HTML template lean at ~1,770 lines (74 KB), and retains all historical turn data losslessly. |
| **ADR-025** | Interactive Sliding-Window Quota Age-Out Timeline & Visual Recovery Gauge | **Adopted** | Implemented continuous 5-hour sliding-window quota headroom projection (`compute_window_recovery_trajectory`) producing 20 uniform 15-minute bucket intervals, threshold recovery milestones (Safe Zone >=50%, Comfortable >=80%, Full 100%), and upcoming turn roll-offs. Visualized via responsive SVG staircase area/line chart in `dashboard/index.html` featuring interactive hover scrubbers, live 30-second client-side clock tick (`setInterval`) with zero page reloads, and tab-focus auto-refresh (`visibilitychange` + dynamic `data.js` script tag reload). |
| **ADR-026** | Watcher Graceful Restart, Self-Healing Keepalive & Atomic Export Sync | **Adopted** | Implemented atomic exporter writes via `.tmp` staging and `os.replace` to eliminate race conditions under concurrent watcher updates; added daemon `KeepAlive` and `ThrottleInterval` keys to LaunchAgent plist; added `setup_service.py restart` via `launchctl kickstart -k` with fallback reload; added live data freshness badge (`● Synced: Just now` / `⚠️ Stale`) and dynamic credit spillover indicator in `dashboard/index.html`. |
| **ADR-027** | 3-Card Single-Row Quota Triad, Marker Time-Clustering & Trajectory Downsampling | **Adopted** | Relocated the 5-Hour Recovery Timeline from an isolated full-width card into the `Antigravity Provider Quota Silos` CSS grid (`repeat(auto-fit, minmax(320px, 1fr))`) as a 3rd `.quota-box` card alongside Gemini and Claude/GPT, saving ~300px vertical space. Replaced blind `[:50]` trajectory slice with milestone-preserving downsampling (`downsample_trajectory_points`) preserving immediate turns, window-closing turns reaching 100%, top 15 token spikes, and 50%/80% threshold crossings across the full 5h horizon. Implemented marker time-clustering within $\pm 4$ minutes in `dashboard/index.html` to prevent circle stacking during rapid prompt bursts. |
| **ADR-028** | Empirical Weekly Quota Exhaustion, 5-Hour Subsumption & Claude/GPT Fallback | **Adopted** | First empirical weekly quota exhaustion event (Tue 8 Sep 2026) calibrates Google's weekly Gemini capacity to **$129.00 USD** (was $133.00). When weekly remaining reaches 0%, the 5-hour burst window is automatically superseded/disabled (upstream server behavior). Track 2 (Claude/GPT) operates independently with zero spillover at 94.7% available, enabling zero-marginal-cost fallback. Dashboard renders dynamic alarm states: red EXHAUSTED badge, disabled 5h bar, highlighted Claude fallback callout, and recovery timeline paused notice. Interactive vs. subagent turn partitioning (model IDs 1050/1322/1132/1301) and daily burn velocity anomaly detection (>25% single-day threshold). |
| **ADR-029** | Dual-Pack Auto-Reload Reconciliation & Multi-Cycle Credit Bank Defense | **Adopted** | Google One AI credit packs auto-reload upon exhaustion. When confirmed cumulative burn reached 3,640 credits (£34.93 / $36.40), Pack 1 (2,500 credits, £23.99) was 100% consumed, triggering Pack 2 auto-purchase (+2,500 credits, £23.99), establishing an active 5,000-credit bank pool (£47.98 / $50.00) with 1,360 credits remaining (£13.05 / $13.60) at 72.8% burn. Configured deterministically across `config/pricing.json`, `data/exhaustion_ledger.json`, and `src/aggregator.py`. |
| **ADR-030** | Empirical Claude Exhaustion Boundaries & Multi-Model Burst Quota Dynamics | **Adopted** | Multi-conversation audit across 132 databases confirms 219 historical Claude turns (18.1M tokens, $46.64 spend). Claude Opus 4.6 (`1026`) exhibits high steady-state cache hit (93.7%), but due to heavy pricing ($15/$1.50/$75 per MTok) and ~$0.20–$0.31/turn cost, continuous unthrottled runs hit rate limits / quota exhaustion within 30 to 95 turns (10–30 minutes), directly triggering Google One statement deductions (-1,445 credits on 08 Sep across `9bf32a2a` and -312 credits on 09 Sep across `14d542c3`). Cross-provider model flips completely invalidate KV caches, incurring 100% cache misses on entry and 70k–80k uncached replay spikes on return. |
| **ADR-031** | Promoted 3-Panel Provider Quota Silos & Runway Governor Hero Deck | **Adopted** | Inverted page layout hierarchy by promoting the 3-panel command deck (`#dual-provider-quotas-section`) directly beneath `<header>` and above `.tab-nav`, relocating the secondary 4-box macro banner and SVG recovery timeline into `#view-quotas`. Implemented 3-panel layout: Left Wing = Gemini Models (weekly allowance + 5h burst); Center Anchor = Weekly Quota Runway & Throttle Governor (dual-benchmark needle bar, Burn Velocity Index [BVI], target daily budget, exhaustion ETA, and zero-shift `h-10` burst countdown clock); Right Wing = Claude & GPT models (weekly allowance + 5h burst). Decoupled telemetry state strictly in `dashboard/data.js` (ADR-024). |
| **ADR-032** | Bayesian M-Estimate Shrinkage & Diurnal Daily-Budget Pacing for Quota Runway | **Adopted** | Resolved cycle-start false alarm where normal evening work (6.1% burned in 2.5h) triggered 4.07x overburn panic. Replaced raw point-slope division with Bayesian M-estimate daily burn shrinkage ($k = 1.0\text{ day}$ pseudo-count at nominal $14.286\%/\text{day}$ baseline) and a diurnal cumulative daily ceiling floor ($Q \le d \times 14.3\%$). Clamps status strictly to nominal green with day-level budget context (`Day 1: 52% of daily budget used`) while preserving instant red alarm triggers for true catastrophic runaways ($50\%+$ in hours). Dynamically bound subtitle CSS text color in `dashboard/index.html`. |
| **ADR-033** | Banked Buffer Surplus Framing & Dual Benchmark Ceiling Tick Marker | **Adopted** | Eliminated semantic ambiguity in Weekly Quota Runway subtitle (`Day 2: 74% of daily budget`) by reframing into explicit Banked Buffer Surplus (`Day 2: +7.4% banked buffer`). Introduced `#runway-ceiling-marker` (dashed slate tick `#94a3b8` at cumulative daily ceiling `left: ${daily_ceiling_pct}%`) onto Sacred Dual Benchmark Bar alongside glowing cyan time needle, and synchronized side-by-side dual legend (`• Time: 24.7% (Day 1.7/7) | Day 2 ceiling: 28.6%`) with zero layout shift. |
| **ADR-034** | Temporal Plan Profiles, Promotional Multipliers & Historical Provenance Engine | **Adopted** | Implemented Unified Temporal Interval schema in `config/pricing.json` (`subscription_history`, `promotions`, `rate_history`, `availability`) with 100% backward compatibility; created deterministic pure-Python temporal resolver (`src/temporal.py`) evaluating turns against timestamp-aware model rates and plan capacities; built Plan Profile & Promotion Manager modal in `dashboard/index.html` supporting plan tier switching, promotional capacity overlays, and JSON config synchronization. |
| **ADR-035** | Interactive "What-If" Workload Simulator & Quota Stress Planner | **Adopted** | Pure-Python simulation engine (`src/simulator.py`) modeling multi-agent turn sequences (tokens, costs, 5h burst exhaustion, weekly runway, and 429 risk). Distinguishes Track 1 Gemini credit spillover at model compute rates (£0.0096/credit) from Track 2 Claude/GPT zero-spillover hard lockouts (ADR-019/ADR-030). Pre-computes 5 standard archetypes (`Deep Refactor Swarm`, `Codebase Audit`, `Rapid Prototyping`, `Claude Opus Stress`, `Subagent Fleet`) in `src/aggregator.py`. Built interactive What-If Simulator tab in `dashboard/index.html` with real-time sliders, safety gauge, plan tier sensitivity table, and SVG trajectory preview. |
| **ADR-036** | Frontend Asset Modularization & External Executive Quota Status Hook | **Adopted** | Decomposed `dashboard/index.html` into `styles.css` (654 lines), `app.js` (2,964 lines), and semantic `index.html` (1,140 lines) preserving 100% offline `file:///` execution and zero-FOUC; implemented atomic external executive quota status hook (`~/.antigravity_quota_status.json`) and fast CLI inspector (`scripts/agy_status.py`, <5ms) for terminal prompts and status bars. |
| **ADR-037** | Subagent Swarm Lineage DAG & Tool Performance Intelligence Architecture | **Adopted** | Extracted parent-child subagent lineages (`invoke_subagent`), tier economics (`flash_lite` mechanical, `flash` engineering, `pro` architecture), and tool execution performance (10k+ invocations, sandbox bypass ratios, error rates, skill attribution) into dedicated engine modules (`src/swarm.py`, `src/tool_analytics.py`) and visual interactive SVG DAG in `dashboard/index.html`; authored comprehensive reverse-engineering whitepaper `docs/FINDINGS.md`. |
| **ADR-038** | Active Quota Attribution, Cache-Efficiency Coaching & Configuration Decoupling | **Adopted** | Partitioned 5-hour rolling window turns into per-session attribution (`active_sessions_5h`) with title/workspace decoration and drawer drill-down; implemented cross-provider model-flip KV cache eviction diagnostics (`compute_cache_coaching_metrics`) quantifying wasted replay tokens and costs; purged scattered hardcoded price fallbacks in favor of `config/pricing.sample.json` and added atomic CLI plan calibrator `scripts/configure_plan.py`. |
| **ADR-039** | Historical Vault Trends & 8–12 Week SVG Trendline Chart | **Adopted** | Pure-Python weekly trends engine (`src/weekly_trends.py`) aggregating turns from `steps_archive` in `data/antigravity_vault.db` into `weekly_cycles_archive`, anchored to Thursday 18:00 UTC resets; reconciles official Google One AI credit overages from `data/exhaustion_ledger.json` (3,640 credits burned); computes Bayesian M-estimate peak BVI; fast-path cache validation via `vault_sync_state` (<0.5ms); interactive 8–12 week SVG trendline chart on the Billing tab with multi-metric toggling (Tokens, Cost & Overage, Peak BVI), crosshairs, and detailed cycles table. |
| **ADR-040** | Standalone Zero-Dependency Telemetry Package Extraction (`antigravity_telemetry`) | **Adopted** | Extracted reverse-engineered wire protobuf decoding and read-only SQLite connector into standalone importable package `antigravity_telemetry/` with pure-Python standard-library-only guarantee, canonical 19-model census without pricing coupling (`models.json`), CLI entrypoint (`python -m antigravity_telemetry dump --json`), standard `pyproject.toml`, and 100% backward-compatible re-export shims in `src/proto_parser.py` and `src/telemetry_reader.py`. |
| **ADR-041** | Google AI Ultra (20 TB / 5x Capacity) Plan Upgrade & Vault Provenance | **Adopted** | Activated Google AI Ultra 5x tier (£79.99/mo / $99.99/mo, $100 5h burst, $645 weekly quota) effective 2026-09-13T17:59:10Z, preserving historical Pro interval in `subscription_history`, adding `subscription_history_archive` table to SQLite Vault (`data/antigravity_vault.db`), and dynamic weekly trends capacity resolution. |
| **ADR-042** | Pre-Flight Workload Budget Checker CLI, Markdown Standup Sharing & Telemetry Precision Hardening | **Adopted** | Implemented deterministic CLI capacity gating (`scripts/agy_quota.py --can-i-run`, exit 0 PROCEED vs exit 1 BLOCKED/429 risk), byte-deterministic Markdown standup digest (`scripts/export_dashboard.py --markdown` + UI clipboard button), lifted conversation ingestion cap across `src/vault.py` (A1), localized browser reset timezones via `Intl.DateTimeFormat` (A2), and cost-weighted overage allocation verification (A5). |
| **ADR-043** | Dynamic Quota Reset Anchoring & Upgrade Day Billing Calibration | **Adopted** | Anchor weekly quota cycles dynamically to the subscription tier's transition timestamp (Sunday 17:58:04 UTC for Ultra 5x, Thursday 18:00 UTC for Pro) and calibrate monthly renewal horizon to Day 13; support explicit live desktop indicator resetTime sync; prune stale projection rows in `subscription_history_archive`. |
| **ADR-044** | CI Offline Fixture Datasets, Deterministic Offline Export Flag & Deferral of Aggregator Modularization | **Adopted** | Formally discarded and indefinitely deferred Item 1 from the Senior Engineering Audit (Aggregator Modularization / C1) because `src/aggregator.py` is stable, 100% test-covered, and provides zero user value to break apart. Added realistic anonymized Antigravity fixtures in `tests/fixtures/antigravity/` with zero PII/user paths, added `--no-live-quota` flag to `scripts/export_dashboard.py` for side-channel-free offline execution, and updated `.github/workflows/ci.yml` to verify extraction and byte-determinism on GitHub Actions. |
| **ADR-045** | Velocity BVI Dual-Line Framing & Ground-Truth Quota Parity | **Adopted** | Eliminated semantic and mathematical ambiguity in Velocity BVI card subtitle ('Day 3: 12.3% over ceiling') by introducing two-tier Format 1: Line 1 signed verdict ('Day 3: -12.8% deficit' vs 'Day 3: +7.4% buffer') and Line 2 arithmetic proof '(55.7% vs 42.9%)' in secondary muted gray; disambiguates percentage points from relative overshoot; responsive mobile/desktop rendering in dashboard/app.js. |
| **ADR-046** | Git Reflog Temporal Branch Attribution & Anti-Bloat Architecture | **Adopted** | Resolved Antigravity static conversation snapshot limitation where in-session branch checkouts collapsed into `main` (inflating `main` to 80%+ turns while hiding 12+ real feature branches). Established Git reflog (`.git/logs/HEAD`) as authoritative temporal source of truth, matching nanosecond-accurate turn timestamps to checkout intervals. Isolated reflog parsing and LRU interval caching into standalone zero-dependency module `src/git_timeline.py` (<320 lines) to strictly prevent `src/aggregator.py` bloat, adding a minimal ~25-line turn-level branch resolution hook. Verified 16 distinct milestone branches emerged with zero token leakage. |
| **ADR-047** | Live Pipeline & Daemon Operational Verification Standard | **Adopted** | Institutionalized mandatory operational gates in `AGENTS.md` and `VDONE.md`. Prohibits declaring milestone completion purely on `--dry-run` test exit codes when backend formulas or data models change. Mandates unsandboxed live export (`export_dashboard.py`) to refresh `dashboard/data.js` against authentic databases, daemon service kickstart (`setup_service.py restart`), and status assertion (`RUNNING / REGISTERED` with valid PID) to ensure UI reflects real production state. |
| **ADR-048** | Light Mode Default Theme Standard & Visual Showcase Elevation | **Adopted** | Established crisp, high-contrast Light Mode (`#f8fafc` canvas, `#ffffff` cards, `#e2e8f0` borders) as the repository's canonical default visual theme across CSS `:root`, browser pre-render bootstrapping, and all 6 high-resolution showcase assets in `docs/assets/`, while maintaining 100% responsive Dark Mode persistence via interactive client-side switching. |
| **ADR-049** | Comprehensive Telemetry Sanitization, Authentic Attribution & Public History Isolation | **Adopted** | Institutionalized automated sanitization regression gates (`tests/test_sanitization.py`) checking zero local user paths, zero leaked conversation UUIDs, zero client/enterprise keywords, and zero unowned custom email domains. Anonymized `pricing.json` renewal day to default 1st of the month, reframed `docs/FINDINGS.md` away from adversarial 'reverse-engineering' to local systems architecture and runtime analysis, attributed authorship authentically to Eneas (disbanded fictional 'Research Group' persona), added explicit employer independence notice in README, and hardened `scripts/publish_to_public.py` with `--reset-history` to permanently purge past leaked blobs from public GitHub commit history. |
| **ADR-050** | Deterministic Synthetic Showcase Demo & Automated GitHub Pages Continuous Deployment | **Adopted** | Engineered a deterministic, zero-PII synthetic telemetry showcase generator (`scripts/build_demo_showcase.py`) synthesizing realistic multi-model agentic telemetry across 4 representative software projects, active Git branches, 25 sessions, turn-by-turn thinking tokens, 3-tier subagent swarm hierarchy DAGs, and tool execution analytics without reading or exposing authentic local SQLite databases. Built frontend demo-mode detection and banner in `dashboard/index.html`, `dashboard/styles.css`, and `dashboard/app.js`. Implemented GitHub Actions continuous deployment workflow (`.github/workflows/deploy_pages.yml`) deploying `dashboard/` to GitHub Pages on pushes to `main` with automated pre-flight sanitization verification. |


---

## 3. Milestone Index

| Milestone | Scope & Deliverables | Status | Verification & Slice Record |
|---|---|---|---|
| **M01: Repository Scaffolding & Spec** | Working agreements (`AGENTS.md`, `CLAUDE.md`), `README.md`, `docs/`, core parser, safe reader, exporter, dashboard UI, and initial test suite. | **Completed** | [`docs/milestones/M01_REPO_INITIALIZATION.md`](milestones/M01_REPO_INITIALIZATION.md) |
| **M02: Rolling Quotas & Subscription Value** | 5h and 1w rolling window bucketing partitioned by `model_id`, sliding window recovery / age-out calculation, subscription allowance vs. credit burn tracking. | **Completed** | [`docs/milestones/M02_ROLLING_QUOTA_ALLOWANCES.md`](milestones/M02_ROLLING_QUOTA_ALLOWANCES.md) |
| **M02b: AI Credit Burn Reconciliation & GBP Multi-Currency** | Hybrid overage state machine, log parser for 429 exhaustion / recovery bookends, model-specific credit rate calibration, hourly activity reconciliation, £23.99 / 2,500 credits calibration and interactive GBP/USD currency toggle. | **Completed** | [`docs/milestones/M02b_AI_CREDIT_BURN_RECONCILIATION.md`](milestones/M02b_AI_CREDIT_BURN_RECONCILIATION.md) |
| **M02c: Quota Cycle Anchoring & Signal-Anchored Overage** | Signal-anchored overage engine eliminating phantom August deductions, Thursday 19:00 BST weekly cycle reset, and 24th monthly billing horizon (£23.99 / 2,500 credits). | **Completed** | [`docs/milestones/M02c_QUOTA_CYCLE_ANCHORING.md`](milestones/M02c_QUOTA_CYCLE_ANCHORING.md) |
| **M03: Live Watcher Daemon & Service Packaging** | Near real-time WAL and runtime log watcher with debouncing, SQLite lock resilience, and macOS LaunchAgent daemon service packaging. | **Completed** | [`docs/milestones/M03_LIVE_WATCHER_SERVICE.md`](milestones/M03_LIVE_WATCHER_SERVICE.md) |
| **M06: Dual-Tier Plan & Overage Credit Architecture** | Decoupled Tier 1 (£18.99/mo subscription) and Tier 2 (£23.99 / 2,500 prepaid overage credit bank), persistent exhaustion ledger (`data/exhaustion_ledger.json`), 4-card UI surface in `dashboard/index.html`, and official statement reconciliation (1,179 credits burned, 1,321 remaining). | **Completed** | [`docs/milestones/M6_DUAL_TIER_PLAN_ARCHITECTURE.md`](milestones/M6_DUAL_TIER_PLAN_ARCHITECTURE.md) |
| **M07: Multi-Model Benchmark Taxonomy & Subagent Discovery** | 14-model empirical benchmark suite across Gemini Flash (3.8/3.7/3.6 at High/Medium/Low), Gemini 3.1 Pro (High/Low), Claude Sonnet 4.6 (Thinking), Claude Opus 4.6 (Thinking), GPT-OSS 120B (Medium), and subagent-routed Gemini Flash Lite (`1050`), calibrated pricing matrix, and comprehensive ledger (`docs/MODEL_BENCHMARKS.md`). | **Completed** | [`docs/milestones/M07_MODEL_BENCHMARK_TAXONOMY.md`](milestones/M07_MODEL_BENCHMARK_TAXONOMY.md) |
| **M08: Telemetry Vault, Lineage Topology & Archival Skill** | Lossless local SQLite vault (`data/antigravity_vault.db`) capturing full text, raw blobs, tool/skill executions, parent-child subagent graphs, log truncation immunity, and global multimodal screenshot ingestion skill. | **Completed** | [`docs/milestones/M08_TELEMETRY_VAULT_ARCHIVE.md`](milestones/M08_TELEMETRY_VAULT_ARCHIVE.md) |
| **M09: Dual-Track Provider Quota Silos (Gemini vs. Claude/GPT)** | Partition telemetry into independent provider quota silos (`gemini` vs `claude_gpt`), compute isolated 5h/weekly allowances and recovery schedules, and mirror Antigravity IDE UI quota surface. | **Completed** | [`docs/milestones/M09_DUAL_TRACK_PROVIDER_QUOTAS.md`](milestones/M09_DUAL_TRACK_PROVIDER_QUOTAS.md) |
| **M10: Cost-Weighted Quota Convergence & Live Desktop Sync** | Implement official Google Developer rate cards, derive model cost proportions, calibrate 5h ($20.00) and weekly ($133.00) capacities, Connect-RPC live quota client (`src/quota_client.py`), standalone CLI (`scripts/agy_quota.py`), multi-model regression harness (`scripts/quota_regression.py`), and offline mathematical fallback. | **Completed** | [`docs/milestones/M10_COST_WEIGHTED_QUOTA_CONVERGENCE.md`](milestones/M10_COST_WEIGHTED_QUOTA_CONVERGENCE.md) |
| **M11: Project & Git Branch Intelligence (Dedicated Dashboard Tab & Branch Costing)** | Extract authoritative workspace URIs and active Git branches from `trajectory_metadata_blob` across all DBs; implement project & branch-level token/cost aggregation in `src/aggregator.py`; build top-level tabbed navigation and dedicated Projects & Git Branches explorer in `dashboard/index.html`. | **Completed** | [`docs/milestones/M11_PROJECT_AND_BRANCH_INTELLIGENCE.md`](milestones/M11_PROJECT_AND_BRANCH_INTELLIGENCE.md) |
| **M12: Subscription Coverage vs. Actual Overage Accounting & Avoided Cost** | Correlate turns with exhaustion intervals at microsecond resolution; attribute confirmed ledger overages strictly to impacted project/branch (`Geminu token consumption dashboard.` on `main`); reframe API spend as Plan Value / Avoided Cost (£203.07 ROI); update portfolio KPIs, table headers, badges, and drawers in `dashboard/index.html`. | **Completed** | [`docs/milestones/M12_SUBSCRIPTION_VS_OVERAGE_ACCOUNTING.md`](milestones/M12_SUBSCRIPTION_VS_OVERAGE_ACCOUNTING.md) |
| **M13: Sliding-Window Recovery Timeline & Live Refresh** | Implemented 5h sliding window recovery trajectory engine (`compute_window_recovery_trajectory`), responsive SVG timeline gauge with threshold benchmarks, live 30s client ticker, tab-focus auto-refresh, and dynamic script reloader in `dashboard/index.html`. | **Completed** | [`docs/milestones/M13_SLIDING_WINDOW_RECOVERY_TIMELINE.md`](milestones/M13_SLIDING_WINDOW_RECOVERY_TIMELINE.md) |
| **M14: Watcher Graceful Restart & Atomic Export Sync** | Implemented atomic `.tmp` export replacements, LaunchAgent `KeepAlive` and `ThrottleInterval` keys, `setup_service.py restart` with `launchctl kickstart -k`, line-buffered watcher logging, and live data freshness sync indicator (`#badge-last-extracted`). | **Completed** | [`docs/milestones/M14_WATCHER_GRACEFUL_RESTART_SYNC.md`](milestones/M14_WATCHER_GRACEFUL_RESTART_SYNC.md) |
| **M15: 3-Card Single-Row Quota Triad & Trajectory Downsampling** | Sited 5-Hour Recovery Timeline into Provider Quota Silos 3-card auto-fit row (`repeat(auto-fit, minmax(320px, 1fr))`); refined to edge-to-edge hero card with expanded 175px SVG chart (`viewBox="0 0 380 160"`, +54% vertical plot area) and streamlined header; fixed `[:50]` trajectory truncation bug via milestone-preserving downsampling (`downsample_trajectory_points`); implemented turn marker time-clustering within $\pm 4$ minutes. | **Completed** | [`docs/milestones/M15_3CARD_QUOTA_TRIAD_TIMELINE.md`](milestones/M15_3CARD_QUOTA_TRIAD_TIMELINE.md) |
| **M16: Empirical Weekly Quota Exhaustion & Alarm States** | Calibrated weekly capacity to \$129.00 from empirical Connect-RPC, implemented exhaustion alarm state engine (`compute_exhaustion_alarm_state`), subagent vs. interactive turn partitioning (`SUBAGENT_MODEL_IDS`, `classify_turn_type`, `compute_subagent_metrics`), daily velocity bucketing with >25% anomaly detection, dynamic 3-card alarm rendering (red EXHAUSTED badge, disabled 5h bar, Claude fallback callout, timeline paused notice), and interactive/subagent breakdown pills. | **Completed** | [`docs/milestones/M16_WEEKLY_QUOTA_EXHAUSTION_ALARM.md`](milestones/M16_WEEKLY_QUOTA_EXHAUSTION_ALARM.md) |
| **M17: Promoted 3-Panel Quota Silos & Runway Governor Hero Deck** | Inverted layout hierarchy promoting 3-panel command deck directly under header above tabs; implemented Left Wing (Gemini), Center Anchor (Weekly Quota Runway with dual-benchmark needle bar, BVI, target daily budget, ETA, and zero-shift 5h burst governor clock), and Right Wing (Claude & GPT models); moved 4-box macro banner and recovery timeline into Quotas tab. | **Completed** | [`docs/milestones/M17_QUOTA_SILOS_RUNWAY_HERO_DECK.md`](milestones/M17_QUOTA_SILOS_RUNWAY_HERO_DECK.md) |
| **M18: Bayesian M-Estimate Shrinkage & Diurnal Budget Pacing** | Resolved cycle-start false alarm via Bayesian M-estimate burn shrinkage ($k = 1.0$) and cumulative daily ceiling floor ($Q \le d \times 14.3\%$). Clamped status to nominal green with day-level budget context (`Day 1: 52% of daily budget used`) and safe exhaustion buffer, while preserving instant red alarms for true catastrophic runaways ($50\%+$ in hours). Dynamically bound subtitle CSS text color in `dashboard/index.html`. | **Completed** | [`docs/milestones/M18_BAYESIAN_RUNWAY_SMOOTHING.md`](milestones/M18_BAYESIAN_RUNWAY_SMOOTHING.md) |
| **M19: Banked Buffer Framing & Benchmark Ceiling Marker** | Replaced ambiguous "Day 2: 74% of daily budget" with explicit Banked Buffer Surplus (`Day 2: +7.4% banked buffer`). Introduced `#runway-ceiling-marker` (dashed slate tick `#94a3b8`) on Sacred Dual Benchmark Bar positioned at cumulative daily ceiling (`daily_ceiling_pct%`) alongside cyan time needle, and synchronized side-by-side dual legend (`• Time: 24.7% (Day 1.7/7) | Day 2 ceiling: 28.6%`). | **Completed** | [`docs/milestones/M19_BANKED_BUFFER_AND_CEILING_MARKER.md`](milestones/M19_BANKED_BUFFER_AND_CEILING_MARKER.md) |
| **M20: Temporal Plan Profiles, Promotional Multipliers & Provenance** | Unified temporal interval schema in `config/pricing.json`, deterministic temporal resolver (`src/temporal.py`), timestamp-aware historical turn costing, promotional multipliers, and Plan Profile & Promotion Manager modal in `dashboard/index.html`. | **Completed** | [`docs/milestones/M20_TEMPORAL_PLAN_PROFILES.md`](milestones/M20_TEMPORAL_PLAN_PROFILES.md) |
| **M21: Interactive "What-If" Workload Simulator & Quota Stress Planner** | Pure-Python deterministic simulation engine (`src/simulator.py`), 5 multi-agent archetypes, aggregator integration (`src/aggregator.py`), plan tier sensitivity comparisons (`pro`, `enterprise_5x`, `ultra_10x`), and interactive What-If Simulator tab with live sliders in `dashboard/index.html`. | **Completed** | [`docs/milestones/M21_WHAT_IF_WORKLOAD_SIMULATOR.md`](milestones/M21_WHAT_IF_WORKLOAD_SIMULATOR.md) |
| **M22: UI/UX Ergonomics & Jobs-Based Tabs Architecture** | 5 jobs-based views (`Now`, `Sessions`, `Projects`, `Billing`, `Plan`), fast telemetry hash-gating engine (`dashboard/meta.js`, 207 bytes) bypassing 12 MB reload when unchanged, clean UI labels eliminating raw LaTeX syntax and ADR badges, professional navigation typography with zero informal emojis, standardized 2-decimal summary currency display (`kpi-imputed-val`), and carousel styling fixes. | **Completed** | [`docs/milestones/M22_UI_UX_JOBS_BASED_TABS.md`](milestones/M22_UI_UX_JOBS_BASED_TABS.md) |
| **M23: Frontend Modularization & External Status Hook** | Modularized `dashboard/index.html` into `styles.css` (654 lines), `app.js` (2,964 lines), and `index.html` (1,140 lines) preserving 100% offline `file:///` zero-dependency execution; atomic external status hook (`~/.antigravity_quota_status.json`); fast CLI inspector (`scripts/agy_status.py`, <5ms); pricing loader consolidation across backend modules; and acceptance gates V107–V112. | **Completed** | [`docs/milestones/M23_FRONTEND_MODULARIZATION_AND_STATUS_HOOK.md`](milestones/M23_FRONTEND_MODULARIZATION_AND_STATUS_HOOK.md) |
| **M24: Subagent Swarm Lineage, Tool Analytics & Reverse Engineering Whitepaper** | Pure-Python swarm lineage DAG builder (`src/swarm.py`) with true-roots deduplication (10 swarms, 24 subagents across 6 projects), token burn velocity & 92.3% cache efficiency, strict avoided cost vs. actual overage segregation (£30.67 avoided vs. £0.00 overage), tool execution analytics engine (`src/tool_analytics.py`), 378-line authoritative whitepaper (`docs/FINDINGS.md`), interactive offline SVG swarm visualizer, project filter & sort controls, and acceptance gates V113–V118. | **Completed** | [`docs/milestones/M24_SUBAGENT_SWARM_AND_FINDINGS.md`](milestones/M24_SUBAGENT_SWARM_AND_FINDINGS.md) |
| **M25: Active Quota Intelligence, Cache Coaching & Clean Config** | 5h burst session attribution drawer on Now tab (M3), cross-provider model-flip KV cache eviction diagnostics on Sessions tab (M5), clean plan configuration template (`config/pricing.sample.json`), CLI calibrator (`scripts/configure_plan.py`), and acceptance gates V119–V124. | **Completed** | [`docs/milestones/M25_ACTIVE_QUOTA_COACHING_AND_CONFIG.md`](milestones/M25_ACTIVE_QUOTA_COACHING_AND_CONFIG.md) |
| **M26: Historical Vault Trends & 8–12 Week SVG Trendline Chart** | Pure-Python weekly trends engine (`src/weekly_trends.py`), `weekly_cycles_archive` in `data/antigravity_vault.db`, Thursday 18:00 UTC cycle boundaries, overage ledger reconciliation (3,640 credits), peak BVI, fast-path cache validation, interactive SVG trendline chart on Billing tab, and acceptance gates V125–V130. | **Completed** | [`docs/milestones/M26_HISTORICAL_VAULT_TRENDS.md`](milestones/M26_HISTORICAL_VAULT_TRENDS.md) |
| **M27: Standalone Importable Package (`antigravity_telemetry`)** | Extract independent zero-dependency telemetry engine (`antigravity_telemetry/`) with pure-Python wire parser, safe SQLite reader (`?mode=ro`), canonical 19-model census, standalone CLI, standard `pyproject.toml`, repo integration, and acceptance gates V131–V135. | **Completed** | [`docs/milestones/M27_PACKAGE_ANTIGRAVITY_TELEMETRY.md`](milestones/M27_PACKAGE_ANTIGRAVITY_TELEMETRY.md) |
| **M28: Google AI Ultra (5x Capacity) Plan Upgrade & Vault Provenance** | Upgraded active subscription tier to Ultra 5x (£79.99/mo, 5x quotas across Gemini and Claude), recorded historical transition at 2026-09-13T17:59:10Z, created `subscription_history_archive` table in `data/antigravity_vault.db`, dynamic weekly capacity resolution, and verified gates V137–V143. | **Completed** | [`docs/milestones/M28_GOOGLE_AI_ULTRA_PLAN_UPGRADE.md`](milestones/M28_GOOGLE_AI_ULTRA_PLAN_UPGRADE.md) |
| **M29: Workload Governance, Standup Digest & Precision Hardening** | Pre-flight budget checker CLI (`--can-i-run`), byte-deterministic standup digest exporter (`--markdown`) and UI clipboard button, unlimited vault ingestion scaling (A1), dynamic browser timezone localization (A2), cost-weighted overage allocation (A5), and acceptance gates V144–V151. | **Completed** | [`docs/milestones/M29_WORKLOAD_GOVERNANCE_AND_PRECISION.md`](milestones/M29_WORKLOAD_GOVERNANCE_AND_PRECISION.md) |
| **M30: Quota Reset Dates & Monthly Renewal Day Calibration** | Dynamic weekly reset cycle bounds (Sunday 17:58:04 UTC for Ultra 5x, Thursday 18:00 UTC for Pro), Day 13 monthly billing renewal, live Connect-RPC `reset_time` synchronization, projection pruning in `subscription_history_archive`, and acceptance gates V152–V158. | **Completed** | [`docs/milestones/M30_QUOTA_DATES_AND_RENEWAL_CALIBRATION.md`](milestones/M30_QUOTA_DATES_AND_RENEWAL_CALIBRATION.md) |
| **M31: CI Offline Fixtures, Deterministic Offline Export & Public Showcase Release** | Anonymized offline test fixtures (`tests/fixtures/antigravity/`), `--no-live-quota` deterministic flag on `export_dashboard.py`, hardened GitHub Actions CI verifying byte-identical exports, formal deferral of Aggregator Modularization (ADR-044), and public repository showcase synchronization (`v1.31.0`). | **Completed** | [`docs/milestones/M31_CI_OFFLINE_FIXTURES_AND_PUBLIC_RELEASE.md`](milestones/M31_CI_OFFLINE_FIXTURES_AND_PUBLIC_RELEASE.md) |
| **M32: Velocity BVI Dual-Line Framing & Buffer Deficit Parity** | Disambiguate BVI card subtitle with two-tier Format 1 (signed deficit/buffer verdict + arithmetic proof operands), responsive styling in app.js, updated aggregator status strings, and acceptance gates V165–V170. | **Completed** | [`docs/milestones/M32_BVI_BUFFER_DEFICIT_FRAMING.md`](milestones/M32_BVI_BUFFER_DEFICIT_FRAMING.md) |
| **M33: Git Reflog Temporal Branch Attribution & Anti-Bloat Architecture** | Establish Git reflog (`.git/logs/HEAD`) checkout transitions as authoritative temporal source of truth; parse microsecond turn timestamps against dynamic checkout intervals; isolate reflog parsing and LRU interval caching into standalone module `src/git_timeline.py` to protect `src/aggregator.py` from bloat; verify emergence of 16 milestone feature branches with zero token leakage; verified acceptance gates V171–V176. | **Completed** | [`docs/milestones/M33_GIT_TEMPORAL_BRANCH_ATTRIBUTION.md`](milestones/M33_GIT_TEMPORAL_BRANCH_ATTRIBUTION.md) |
| **M34: README v2.0, Light Mode Standard & Visual Showcase Overhaul** | Modernize documentation to v2.0 with 6 capability pillars; elevate Light Mode as default across CSS `:root` and client initialization; regenerate 6 high-resolution Light Mode showcase screenshots in `docs/assets/`; synchronize metrics to Ultra 5x, dynamic reset cycles, Day 13 renewal, and 185 passing tests; designated Major Release `v2.0.0` (internal milestone `v1.34.0`) published to public showcase repo; verified acceptance gates V177–V182. | **Completed** | [`docs/milestones/M34_README_V2_AND_LIGHT_THEME.md`](milestones/M34_README_V2_AND_LIGHT_THEME.md) |
| **M35: Privacy Hardening, Whitepaper De-jargonizing & Clean Public Release** | Scrub conversation UUIDs, titles, and enterprise references from `docs/reports/`; anonymize personal billing and schedule dates in `docs/WEEKLY_QUOTA_EXHAUSTION_REPORT.md` and `config/pricing.json`; update author email to GitHub no-reply; retitle whitepaper `docs/FINDINGS.md` away from adversarial 'reverse-engineering' to local systems architecture; add 5-gate sanitization regression suite `tests/test_sanitization.py`; add `--reset-history` flag to `scripts/publish_to_public.py` to eradicate leaked commits from public history; add plain-English opening and employer independence disclaimer to `README.md`; verified acceptance gates V183–V188. | **Completed** | [`docs/milestones/M35_PRIVACY_HARDENING_AND_PUBLIC_RELEASE.md`](milestones/M35_PRIVACY_HARDENING_AND_PUBLIC_RELEASE.md) |
| **M36: Live Interactive Showcase & GitHub Pages Deployment** | Pure-Python deterministic synthetic demo generator (`scripts/build_demo_showcase.py`) with 4 projects, active Git branches, 25 multi-model sessions, turn-by-turn thinking tokens, 3-tier subagent swarm hierarchy DAG, and 18 tool categories; frontend demo mode banner and responsive GitHub repository linking; GitHub Actions automated deployment pipeline (`.github/workflows/deploy_pages.yml`) with pre-flight sanitization and demo schema verification gates; verified acceptance gates V189–V194. | **Completed** | [`docs/milestones/M36_GITHUB_PAGES_LIVE_SHOWCASE.md`](milestones/M36_GITHUB_PAGES_LIVE_SHOWCASE.md) |

---

## 4. Open Work Register & Deferred Items

1. **Model ID Taxonomy Expansion (`config/pricing.json`)**:
   - Status: Calibrated IDs `1318` (3.8 Flash High), `1319` (3.8 Flash Medium), `1320` (3.8 Flash Low), `1298` (3.7 Flash High), `1299` (3.7 Flash Medium), `1300` (3.7 Flash Low), `1071` (3.6 Flash High), `1072` (3.6 Flash Medium), `1073` (3.6 Flash Low), `1050` (Flash Lite Subagent), `1016` (3.1 Pro High), `1036` (3.1 Pro Low), `1035` (Claude Sonnet 4.6 Thinking), `1026` (Claude Opus 4.6 Thinking), and `342` (GPT-OSS 120B Medium).
   - Evidence: Grounded via empirical 2-turn coding benchmark documented in [`docs/MODEL_BENCHMARKS.md`](MODEL_BENCHMARKS.md).
   - Actionable Plan: Calibrate remaining candidate IDs (`1322`, `1132`) as benchmarks run.
2. **Authoritative Project & Git Branch Extraction (M11 / ADR-022)**:
   - Status: Completed. Authoritative root paths and active Git branches extracted directly from `trajectory_metadata_blob` across all DBs; dedicated Projects & Git Branches tab and portfolio KPIs built in `dashboard/index.html`. Gates V42–V47 verified.
3. **Subscription Coverage vs. Actual Overage Accounting (M12 / ADR-023)**:
   - Status: Completed. Correlated turns with exhaustion intervals at microsecond resolution; attributed confirmed ledger overages strictly to impacted project/branch (`Geminu token consumption dashboard.` on `main`); reframed API spend as Plan Value / Avoided Cost (£203.07 ROI); updated portfolio KPIs, table headers, badges, and drawers in `dashboard/index.html`. Gates V48–V52 verified.
4. **Sliding Window Token Age-Out Visualization (M13 / ADR-025)**:
   - Status: Completed. Responsive interactive SVG timeline chart with staircase recovery projections, 30s client clock ticks, tab-focus auto-refresh, and decoupled `data.js` reloading. Gates V53–V57 verified.
5. **Diurnal Smoothing & Daily Budget Pacing for BVI Runway (M18 / ADR-032)**:
   - Status: Completed. Implemented Bayesian M-estimate daily burn shrinkage ($k = 1.0\text{ day}$) and diurnal cumulative daily ceiling floor ($Q \le d \times 14.286\%$) in `src/aggregator.py`; bound dynamic subtitle colors and contextual day usage (`Day 1: 52% of daily budget`) in `dashboard/index.html`. Gates V74–V77 verified.
6. **Temporal Plan Profiles & Historical Provenance Engine (M20 / ADR-034)**:
   - Status: Completed. Unified temporal schema, standalone temporal resolver (`src/temporal.py`), timestamp-aware turn costing, and interactive Plan Profile & Promotion Manager modal (`dashboard/index.html`). Gates V82–V87 verified.
7. **Interactive What-If Workload Simulator & Quota Stress Planner (M21 / ADR-035)**:
   - Status: Completed.
   - Evidence: Pure-Python deterministic simulation engine (`src/simulator.py`), 5 multi-agent archetypes, aggregator integration (`src/aggregator.py`), plan tier sensitivity comparisons (`pro`, `enterprise_5x`, `ultra_10x`), and interactive What-If Simulator tab with live sliders in `dashboard/index.html`. Gates V88–V94 verified cleanly.
8. **UI/UX Ergonomics & Jobs-Based Tabs Architecture (M22 / ADR-031 / ADR-035)**:
   - Status: Completed.
   - Evidence: 5 jobs-based views (`Now`, `Sessions`, `Projects`, `Billing`, `Plan`), fast telemetry hash-gating ticker (`meta.js`), clean typography, and zero informal emojis. Gates V101–V106 verified cleanly.
9. **Frontend Modularization & External Status Hook (M23 / ADR-036)**:
   - Status: Completed.
   - Evidence: Decomposed `dashboard/index.html` into `styles.css` (654 lines), `app.js` (2,964 lines), and `index.html` (1,140 lines); atomic external status hook (`~/.antigravity_quota_status.json`); fast CLI inspector (`scripts/agy_status.py`, <5ms); pricing loader consolidation; gates V107–V112 verified cleanly.
10. **Subagent Swarm Lineage, Tool Analytics & Findings Whitepaper (M24 / ADR-037)**:
    - Status: Completed.
    - Evidence: Pure-Python swarm extraction (`src/swarm.py`) with true-roots deduplication (10 swarms, 24 subagents across 6 projects), token burn velocity (107k toks/turn) and 92.3% cache efficiency; tool performance engine (`src/tool_analytics.py`, 10,197 tool calls across 23 tools); 378-line authoritative whitepaper `docs/FINDINGS.md`; interactive offline SVG DAG, project filter & multi-criteria sort in `dashboard/index.html`; gates V113–V118 verified cleanly.
11. **Active Quota Attribution, Cache Coaching & Config Decoupling (M25 / ADR-038)**:
    - Status: Completed.
    - Evidence: 5h burst session attribution drawer (`active_sessions_5h`), cross-provider model-flip diagnostics (`compute_cache_coaching_metrics`), `config/pricing.sample.json`, `scripts/configure_plan.py`; gates V119–V124 verified cleanly.
12. **Historical Vault Trends & 8–12 Week SVG Trendline Chart (M26 / ADR-039)**:
    - Status: Completed.
    - Evidence: Pure-Python weekly trends engine (`src/weekly_trends.py`), `weekly_cycles_archive` table in `data/antigravity_vault.db`, Thursday 18:00 UTC cycle boundaries, Google One credit overage reconciliation (3,640 credits), peak BVI calculation, `<0.5ms` turn-count fast-path cache validation, and interactive 8–12 week SVG trendline chart on Billing tab with multi-metric toggles; gates V125–V130 verified cleanly.
13. **Aggregator Modularization (Senior Audit §2.2 C1 / Item 1)**:
    - Status: Discarded & Deferred Indefinitely (ADR-044).
    - Rationale: High-risk code churn on a rock-solid, production-tested module. `src/aggregator.py` is 100% verified under 170+ unit tests. Splitting into submodules yields zero new capabilities or user-facing value while introducing significant regression surface. Architectural stability is formally prioritised over aesthetic line-count reduction.




---




